import HistContent from '../../components/_base/HistContent.vue'

describe('HistContent.vue', () => {
  it('should have "HistContent" as name', () => {
    expect(HistContent.name).toBe('HistContent')
  })
})
